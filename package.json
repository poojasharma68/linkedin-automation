{
  "name": "linkedin-post-automation",
  "version": "1.0.0",
  "description": "Production-ready LinkedIn Post Automation System",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently -k -n server,client -c blue,green \"node --watch backend/server.js\" \"npm --prefix client run dev\"",
    "dev:server": "node --watch backend/server.js",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "serve": "npm run build:client && node backend/server.js",
    "lint": "node --check backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "linkedin",
    "automation",
    "puppeteer",
    "mongodb"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "puppeteer": "^24.4.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "sharp": "^0.33.5",
    "winston": "^3.17.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
